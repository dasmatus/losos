# nix-direnv + devenv: enter the dev environment on cd.
#   direnv allow
# `use devenv` and not `use flake`: devenv runs standalone here, because
# devenv.lib.mkShell cannot evaluate purely inside a flake. See the inputs
# block in flake.nix and the header of devenv.yaml.
use devenv
