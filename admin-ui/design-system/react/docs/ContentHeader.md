---
category: Chrome
---

Page heading row inside `Content` (`div.content-header`): an `h1` from `title`, with optional right-aligned children (typically a `StatusDot` or a small action).

```tsx
<ContentHeader title="Settings">
  <StatusDot state="up" label="lososd running" />
</ContentHeader>
```
