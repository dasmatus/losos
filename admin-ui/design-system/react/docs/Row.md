---
category: Forms
---

One settings row inside `Group` (`div.row`, min-height 44px): label content on the left, the control on the right. Use `className="row-label"` on the label span, or let a labeled `Switch` fill the whole row.
