---
category: Display
---

A paragraph row (`p.chip-row`) holding one or more `Chip`s, used inside `Card` to list the service's addresses.

```tsx
<ChipRow>
  <Chip>philae.local/forgejo</Chip>
  <Chip>ssh://git@philae.local</Chip>
</ChipRow>
```
