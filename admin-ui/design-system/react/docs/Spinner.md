---
category: Feedback
---

14px spinning progress ring (`span.spinner`). Inside a `ProgressCard` the stylesheet morphs it automatically: `done` turns it into a green ✓, `failed` into a red ×. `ProgressCard` already renders it, so use `Spinner` directly only for small inline waits.
